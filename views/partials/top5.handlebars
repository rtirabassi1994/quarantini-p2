<h1 class = "subheader">The Most Popular Drinks</h1>

  {{#each top5Drinks}}
  <div class="card" style="width: 18rem;">
    <div class="card-body">
    <div class="col text-center">
      <a href =""><h5 class="card-title">{{this}}</h5></a>
        <a href="#" class="btn btn-primary">Save for Later</a>
      </div>
    </div>
  </div>
{{/each}}
